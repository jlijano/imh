const header=document.querySelector('.site-header');const toggle=document.querySelector('.menu-toggle');function setMenu(open){if(!header||!toggle)return;header.dataset.open=String(open);toggle.setAttribute('aria-expanded',String(open));}toggle?.addEventListener('click',()=>setMenu(header?.dataset.open!=='true'));document.querySelectorAll('.site-nav a').forEach(link=>link.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',event=>{if(event.key==='Escape')setMenu(false)});window.addEventListener('resize',()=>{if(window.innerWidth>1024)setMenu(false)});const search=document.querySelector('[data-department-search]');const category=document.querySelector('[data-category-filter]');const departments=[...document.querySelectorAll('.department')];function filterDepartments(){const q=(search?.value||'').toLowerCase();const cat=category?.value||'all';departments.forEach(card=>{const matchesText=!q||card.textContent.toLowerCase().includes(q);const matchesCat=cat==='all'||card.dataset.category===cat;card.dataset.hidden=String(!(matchesText&&matchesCat));});}search?.addEventListener('input',filterDepartments);category?.addEventListener('change',filterDepartments);
